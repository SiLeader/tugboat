# /etc/tmpfiles.d/tugboat-flannel.conf
# /run/flannel is recreated on each boot.
# FLANNEL_NETWORK is rendered from install-worker.sh --cni-subnet.
d /run/flannel 0755 root root -
f /run/flannel/subnet.env 0644 root root - FLANNEL_NETWORK=${CNI_SUBNET}
